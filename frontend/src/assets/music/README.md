Drop background music here (`.mp3`, `.ogg`, `.m4a`). Every file becomes a track in
the BGM player; the file name (minus extension, dashes/underscores → spaces) is
shown as the track title. Order is alphabetical, so prefix with `01-`, `02-` … to
control it. Rebuild after adding files.
